Correct. Cursor has a dedicated `/pricing` page. The home page is entirely product — what it does, how it feels, social proof. No pricing section anywhere.

Here’s the corrected prompt:

-----

**Build a landing page for Aiglos, an MCP security runtime. Model the design closely after cursor.com/home.**

**Design language — match cursor.com/home exactly:**

- Near-white background (`#fafaf9`)
- Black/near-black text (`#0a0a0a`)
- Clean sans-serif — use Inter or Geist
- Minimal nav, extreme whitespace
- Large full-bleed product UI panels do all the visual work
- Subtle borders, no gradients, no glow effects, no dark themes except inside the product panels
- Feature sections alternate: text left / UI right, then text right / UI left

**Nav:** Logo “Aiglos” left. Links: Product / Docs / Blog / Pricing. Buttons: Sign In (ghost) + Download (black filled, links to install docs)

**Hero:**

- Big centered headline: “Secure every tool call your AI agents make.”
- One-line subhead: “Aiglos intercepts MCP tool calls in-process, before execution. One import. Zero config.”
- Single CTA button: “Get Started Free”
- Below the CTA, full-bleed dark UI panel: a terminal-style live interceptor feed showing tool calls streaming in with CLEAN / WARN / BLOCK verdict badges, timestamps, tool names. This is the product screenshot.

**Logo strip:** “Works with LangChain · LlamaIndex · AutoGen · CrewAI · n8n · Any MCP client”

**Three feature sections** (each: headline + 2–3 sentences of body copy + a dark UI panel on the alternating side):

1. “In-process. No proxy. No port.” — UI panel shows a code snippet: `import aiglos` and a comment explaining it patches into mcp.client.session at import time
1. “36 rule families. Under a millisecond.” — UI panel shows a clean grid of rule family labels (T01–T06 Injection, T07–T12 Shell, T13–T18 SSRF, etc.) with live verdict counts ticking up
1. “Attestation artifacts. Signed and verifiable.” — UI panel shows a formatted JSON artifact with artifact_id, session summary, and an RSA signature block

**OSS callout — simple centered text section:**
“The detection engine is MIT licensed.”
“The attestation layer funds the research.”
Two links: View on GitHub → and Read the Docs →

**Footer:** Logo left, links center (Docs / GitHub / CVEs / Pricing / Contact), “[aiglos.io](http://aiglos.io)” right

**Hard constraints:**

- No pricing section on this page — pricing lives at /pricing
- No ARR numbers, no investor language, no business strategy
- Single HTML file, vanilla JS only
- Mobile responsive
- The only dark surfaces are the product UI panels — everything else is light​​​​​​​​​​​​​​​​