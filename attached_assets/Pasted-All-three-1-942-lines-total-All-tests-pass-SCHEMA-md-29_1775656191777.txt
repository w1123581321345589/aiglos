All three. 1,942 lines total. All tests pass.

**SCHEMA.md (292 lines)** is the governing document. It defines the five page types with YAML frontmatter schemas, the four operations (INGEST, PROPOSE, LINT, EVOLVE) with exact step-by-step instructions, the cross-referencing conventions, the proposal promotion workflow, and the lint checklist. This is the CLAUDE.md equivalent — the file that makes the LLM a disciplined wiki maintainer rather than a generic chatbot. The boundary it enforces most clearly: the wiki produces proposals, it does not write match functions.

**wiki.py (999 lines)** is the core engine. Ingest routes incoming raw intel to relevant rule pages using the T-rule keyword map, creates incident pages for breach sources, runs five gap checks against novel attack vectors not yet in T01-T95, and appends to INDEX.md and LOG.md on every operation. The five proposed new rules it identified from test data: T96 MODEL_WEIGHT_POISON, T97 MCP_TOOL_SCHEMA_INJECT, T98 CONTEXT_WINDOW_EXFIL, T99 CREDENTIAL_SELF_ROTATION, T100 LOCAL_MODEL_BACKDOOR. Each proposal carries enough structured content that a developer can implement the match function directly. Lint surfaces unvalidated rules, stale citations, overdue proposals, and orphan pages without auto-fixing. Evolve runs the full cycle on a heartbeat.

**feeds.py (656 lines)** covers NVD CVE API, GitHub Security Advisories, MITRE ATLAS, and configurable RSS feeds including Trail of Bits, Krebs, SANS ISC, and Google Security Blog. Every connector normalizes to the same RawItem envelope with slug, source type, severity, CVE IDs, CWE IDs, MITRE IDs, and structured content. Each item is written to raw/ with a stable content-addressed filename so re-runs never duplicate. The AI relevance filter on RSS keeps the wiki focused — it only ingests posts matching keywords like agent, llm, supply chain, injection, prompt, mcp.

The test run produced exactly the right behavior: T81-related supply chain intel touched 2 rule pages directly. The backdoor/local model post generated 3 proposals for genuinely novel attack vectors. The MCP schema post generated 2 more. Five pending proposals in the queue after three raw items — all of them novel, none of them redundant with existing rules.​​​​​​​​​​​​​​​​