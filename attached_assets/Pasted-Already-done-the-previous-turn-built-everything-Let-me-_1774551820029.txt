Already done — the previous turn built everything. Let me copy to outputs and run the final verification.​​​​​​​​​​​​​​​​

**20/20. v0.25.8.**

-----

Here’s the full state of what shipped across this session and the previous one, for the push:

**What’s in v0.25.8:**

`declare_studio_pipeline(guard, roles, studio_name, allow_deploys, coordinator_role)` — one call registers up to 24 named roles with pre-scoped tools and hard bans. The 48-agent Claude Code Game Studios pipeline goes from generating T38 noise on every spawn to clean signal: declared roles in scope score 0.0, declared roles outside scope score 0.90, undeclared spawns score 0.78. The Art Director can’t deploy. The QA Lead can’t push to main. The Sound Designer can’t touch source. Every role gets `fabricate_data` and `unverified_claims` banned automatically.

`STUDIO_ROLE_TOOLS` — the 25-role configuration map. Art Director, Creative Director, Game Designer, Narrative Designer, Lead/Engine/Gameplay/AI/Tools Programmer, Level Designer, Environment/Character Artist, UI Designer, VFX Artist, Sound Designer, Composer, Audio Programmer, QA Lead/Tester, Producer, Project Manager, Researcher, Writer, Localization, `_default` fallback. Each carries the minimum necessary tool list, explicit filesystem scope, and hard bans appropriate to the role’s function.

`claude-code-game-studios`, `ccgs`, and `game-studio` added to `KNOWN_AGENTS` — 17 total. The T38 orchestrator detection now recognizes Game Studios pipelines automatically.

`scaffold.py` updated with game studio role keywords — `aiglos agents scaffold` produces correct `declare_subagent()` calls when a developer describes their pipeline in natural language.

`dm_donchitos.md` — the outreach DM is ready. Send it same day as the launch post, after the Game Studios tweet gets traction. The message leads with the concrete problem (T38 noise on 48 spawns), delivers the fix (one call), and ends with the T78 cascade observation that no other tool has named. No ask. That’s the right structure for an open source cold outreach.

**The T78 cascade point is the most strategically useful thing in the DM.** Donchitos almost certainly hasn’t thought about cross-agent confidence amplification as a security property. “Art Director describes vague style → Level Designer adds confidence → QA Lead validates the now-wrong spec” is an observation that makes someone stop and think. It’s not in any other security tool’s vocabulary. It becomes a reason to cite Aiglos in the Game Studios documentation, which is a distribution flywheel with 3,000 stars and rising.

**NIST.** April 1. [regulations.gov](http://regulations.gov). nist-submission.md. Six days.​​​​​​​​​​​​​​​​