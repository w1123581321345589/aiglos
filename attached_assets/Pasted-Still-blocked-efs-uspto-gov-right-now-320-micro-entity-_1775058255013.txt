Still blocked. **efs.uspto.gov right now. $320 micro entity. Save receipt. Then NIST with T-rule count 89 after this build. Deadline April 2.**

Building all three in parallel.​​​​​​​​​​​​​​​​

7/7. Now T89 and the README compliance section:​​​​​​​​​​​​​​​​

**22/22.** Copying all outputs:​​​​​​​​​​​​​​​​

**22/22. v0.25.18. 89 rules.**

-----

Here is exactly what was built:

**T85 extended with undercover mode vocabulary.** The original T85 fired on explicit denial keywords like “claim to be human” and “never admit you are an AI.” The undercover mode details from the leak revealed a second, structurally different pattern: Anthropic’s own system injecting suppression language automatically, not because a user configured it, but because the target repo is not on an internal allowlist. The strong indicators list now includes the exact phrases from undercover.ts: “operating undercover in a public/open-source repository,” “never include any hint that you are an AI,” “strips all traces,” “no force-off,” “internal allowlist,” and “suppress all attribution.” All four new undercover mode test cases pass alongside the existing ones.

**T89 CONTRIBUTION_PROVENANCE_SUPPRESSION** (rule 46, score 0.78) — the new rule that catches the action side, not just the config side. T85 catches suppression in configuration files. T89 catches suppression at the moment of the VCS operation itself: a git commit, push, PR, or merge where undercover mode indicators are present in the args. Three detection vectors: a named VCS tool (git.commit, github.create_pr, etc.) with suppression in args; a bash/shell execution of git commands with suppression context; and a commit/PR tool whose args contain the verbatim undercover mode prompt language that Anthropic injects. Score is 0.78 rather than higher because a legitimate workflow exists — a human reviews AI suggestions and commits them manually without any suppression — so the rule requires the suppression indicator to be present in the same tool call, not just anywhere in the session.

**README compliance section on undercover mode.** A new section before the compliance table explains exactly what undercover mode is, that it is auto-on with no force-off, what it suppresses, and that Aiglos’s ForensicStore is the only audit trail that survives it. Includes the specific query (`aiglos forensics query --threat T89`) and maps it to NDAA §1513, EU AI Act Article 52, and enterprise open-source contribution policies. The T85 and T89 rows in the rules table are updated. Badge reads 89 rules.

**The enterprise sales moment this creates.** Any enterprise with an open-source contribution policy — which is nearly every enterprise — now has a specific, answerable compliance question about Claude Code. “Your developers are using Claude Code to contribute to public repos. Those contributions look human. There is no way to disable this. When your compliance team or a regulator asks what AI contributed to your public codebase last quarter, `aiglos forensics query --threat T89` is the only way to answer.” That is a one-sentence close for enterprise security buyers.

**NIST submission T-rule count is now 89. File the provisional first.**