Seven changes:
Hero stats bar — "10 Campaign patterns / 515 Federal deadline" → "13 Inspection triggers / 0 Required dependencies." The "515 Federal deadline" was a leftover from the NDAA era with no context. Zero required dependencies is one of the most important facts about the product — it belongs in the hero. "13 Inspection triggers" reflects the actual count and gestures at the intelligence depth.
Terminal version — v0.7.0 → v0.13.0. This was the first thing anyone reading the code example would notice.
Stats strip — "10 Published CVEs / 3 Surfaces / 10 Campaign patterns / 0 Other tools" → "882 Tests passing / 3 Surfaces / 13 Inspection triggers / 0 Required dependencies." 882 tests is a credibility signal for a security product — it answers "how do I know this works." The CVE count (10) is still accurate but belongs in the CVE section, not the hero strip. "13 Inspection triggers" and "0 dependencies" replace the weaker prior stats.
Step 3 copy — "Eight inspection triggers" → "13 inspection triggers" with a line about behavioral baseline. The step tag changed from "Self-improving rules" to "Learns your deployment" — more precise, more differentiating.
Bento adaptive card — "Eight inspection triggers" → "13 inspection triggers" with behavioral anomalies and global prior match included in the description.
Campaign grid — "New · v0.7.0" badge removed from EXTERNAL_INSTRUCTION_CHANNEL (it's not new anymore), REPEATED_INJECTION_ATTEMPT added with "New · v0.8.0" badge. Grid now shows all 10 patterns. The badge progression is accurate.
Three new bento cards — one each for Behavioral Baseline, Policy Proposals, and Federated Intelligence. Each uses a distinct accent color (green, amber, purple) to differentiate from the existing orange-toned cards. Each has a code block that makes the concrete output visible in the hero position rather than requiring the reader to reach the docs. The baseline card shows the key insight in two lines: same T19 call, devops-agent scores LOW, code-agent scores HIGH — different baselines. The proposals card shows the CLI output that replaces webhook fatigue. The federation card shows the local weight progression and states the privacy guarantee in one line.