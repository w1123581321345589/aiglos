Mapping the full build before writing a line:

- **B.1** `aiglos/core/federation.py` — `FederationClient`: `extract_shareable_transitions()`, `push_transitions()`, `pull_global_prior()`, Laplace differential privacy noise
- **B.2** `aiglos/core/global_prior.py` — `GlobalPrior` dataclass, `merge_into(predictor)`, weighted local/global blending that shifts from 20% local at session 1 to 80% local at 100+ sessions
- **B.3** `aiglos/core/intent_predictor.py` — `warm_start_from_prior()`, prior version tracking
- **B.4** `aiglos/adaptive/inspect.py` — 13th trigger `GLOBAL_PRIOR_MATCH`
- **B.5** `aiglos/__init__.py` — v0.13.0, exports, `enable_federation=True` kwarg in `attach()`
- **B.6** `tests/test_federation.py` — ~45 testsGood. Now I have the complete picture. Building:Now write the test suite:64/64. Full suite:**882/882. All three tracks complete.** Copy everything:**882/882. Tracks A, B, and C all complete.**

---

**Track A — Behavioral Baseline (v0.11.0, 65 tests):**

`behavioral_baseline.py` — Per-agent statistical fingerprinting across three feature spaces: event rate (z-score), surface mix (chi-squared), rule frequency distribution (symmetric KL-divergence). `AgentBaseline`, `BaselineEngine`, `BaselineScore`, `SessionStats`. Cold-start returns LOW with `baseline_ready=False`. MCP event reconstruction from `total_events - recorded_surface_events`.

`observation.py` — `agent_baselines` table, `upsert_baseline()`, `get_baseline()`, `get_agent_session_stats()` with MCP reconstruction, `agent_session_count()`, `baseline_anomaly_stats()`.

`inspect.py` — 11th trigger `BEHAVIORAL_ANOMALY`: fires on 2+ HIGH or 3+ MEDIUM anomaly sessions in the last 10. Amendment candidate.

`openclaw.py` — `enable_behavioral_baseline()`, close-session scoring, `ArtifactExtensions.baseline` field.

---

**Track B — Federated Intelligence (v0.13.0, 64 tests):**

`federation.py` — `FederationClient` with push/pull, Laplace differential privacy noise (epsilon=0.1), rate limiting (one push per hour), TTL-based prior caching, hard timeout (8s) on all network calls, graceful degradation on every failure mode. `extract_shareable_transitions()` — extracts only noisy unigram counts over the 39-element rule vocabulary, deliberately excludes bigrams to reduce information density. `GlobalPrior` with `merge_into(predictor)` — local weight schedule from 20% at 0 sessions to 80% at 100+ sessions using smooth S-curve interpolation.

`intent_predictor.py` — `warm_start_from_prior()`, `prior_version` property, `_prior_version` init tracking.

`inspect.py` — 13th trigger `GLOBAL_PRIOR_MATCH`: early-warning for novel attack classes spreading across the network. Fires only for agents with thin local models (<5 sessions) where the global prior predicts a HIGH-probability threat. `_find_cached_prior()` loads from `~/.aiglos/global_prior.json`.

`openclaw.py` — `enable_federation(api_key, endpoint)`, close-session push hook, immediate warm-start pull at enable time.

---

**Track C — Policy Proposals (v0.12.0, 65 tests):**

`policy_proposal.py` — `BlockPattern` (evidence accumulator with confidence scoring), `PolicyProposalEngine` (tracks patterns across sessions, generates proposals at threshold), `PolicyProposal` (four types: LOWER_TIER → RAISE_THRESHOLD → ALLOW_LIST → SUPPRESS_PATTERN, selected by evidence tier). Confidence formula accounts for repetition, consistency, baseline confirmation, and incident count. 30-day auto-expiry. Full approve/reject/rollback lifecycle.

`observation.py` — `block_patterns` table (raw Tier 3 events with rolling window queries), `policy_proposals` table (full lifecycle), eight new methods.

`inspect.py` — 12th trigger `REPEATED_TIER3_BLOCK`: queries block_patterns table, skips patterns with existing pending proposals, generates PolicyProposal directly.

`cli.py` — `aiglos policy list|show|approve|reject|stats` with confidence bars, repetition counts, days remaining.

---

**The compounding effect as designed:** Behavioral baseline catches what rules miss (agent-specific anomaly). Policy proposals eliminate the per-action approval death spiral (one decision replaces infinite interruptions). Federation makes both better with scale (every deployment contributes to the global prior, every deployment benefits from what others encounter). The three tracks are independently useful but multiply together.