Now add `rule_id` as a proper field on `AgentDefViolation` and move T36/T38 checks before the Tier 1 early return:​​​​​​​​​​​​​​​​

Now the critical fix: T36/T38 checks run after Tier 1 auto-allow. Need to move them before it by checking them early in `inspect_subprocess`. The cleanest fix is to run T36/T38 before the `classify_tier` short-circuit:​​​​​​​​​​​​​​​​

Now remove the duplicate T36/T38 checks that appear later in the function:​​​​​​​​​​​​​​​​

71/71. Now run the full suite:​​​​​​​​​​​​​​​​

Two issues: version check is stale (easy fix in test), and T37 needs to respect the allow-list. Stripe is being blocked even when it’s allow-listed. Let me fix T37 to check `allowed` before blocking:​​​​​​​​​​​​​​​​

Now update the version assertion in the old test:​​​​​​​​​​​​​​​​

251/251. Let me copy the updated files to outputs:​​​​​​​​​​​​​​​​

**251/251 passing. v0.3.0 is clean.**

Here’s what was built and the bugs that got fixed along the way:

**Five capabilities shipped:**

1. **T36_AGENTDEF** (subprocess layer): Detects writes to `~/.claude/agents/`, `.cursor/rules/`, `~/.openclaw/`, `SOUL.md`, `IDENTITY.md`, etc. as Tier 3 GATED. Read access is Tier 2 MONITORED. The critical fix was moving this check before the Tier 1 auto-allow gate, because commands like `cat SOUL.md` match the read-only Tier 1 pattern and would have slipped through without the early intercept.
1. **T38 AGENT_SPAWN** (subprocess layer): Detects `claude code --print`, `openclaw run`, `python *agent*.py`, `node *agent*.mjs`, agency-agents install scripts. Same early-intercept fix applied. The original pattern had missing pipe operators between several alternations due to how multiline string concatenation works in Python without explicit `|`.
1. **T37 FIN_EXEC** (HTTP layer): Splits into hard payment hosts (Stripe, PayPal, Square, ACH) that block any POST, and RPC hosts (Infura, Alchemy) that only block when the body contains `eth_sendTransaction` or `eth_sendRawTransaction`. `eth_call` passes through. Allow-listed hosts are fully exempt.
1. **`multi_agent.py`** (new module): `AgentDefGuard` snapshots and diffs agent definition files; `SessionIdentityChain` HMAC-signs every event with a session-scoped key; `MultiAgentRegistry` tracks the full parent-child spawn tree in the session artifact.
1. **`__init__.py` v0.3.0**: New `attach()` params (`enable_multi_agent`, `guard_agent_defs`, `session_id`), `close()` runs a final AgentDefGuard check and merges the spawn tree into the artifact, `status()` exposes all three new layers.​​​​​​​​​​​​​​​​