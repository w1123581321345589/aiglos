First — NIST deadline is now in the function list. One more attempt before the build.​​​​​​​​​​​​​​​​

The reminder tool is broken in this environment — consistent across the whole session. Flag April 1 manually, [regulations.gov](http://regulations.gov), nist-submission.md is ready to paste.

Now the actual question — and you’re right that it’s bigger than `aiglos init`.

-----

**The implication isn’t just a setup command. It’s a distribution strategy.**

The tweet describes a problem that every one of 311,000 OpenClaw users has: “Default OpenClaw is useless. You have to train it like an intern on day one.” The author spent a weekend building soul.md, learnings.md, heartbeat.md, tools.md, agents.md, and two-agent architecture by hand. His conclusion: “Bookmark this. A few hours to set up. Compounds for years.”

That’s the pain point. If Aiglos solves it in one command — `aiglos launch` — then Aiglos becomes the answer to the thing every serious OpenClaw user is already searching for. Not “the security tool you add later” but “the thing that makes OpenClaw actually work from day one, with security built in by default.”

The positioning shift: from “security for OpenClaw” to “OpenClaw, production-ready in 5 minutes.” The security isn’t the headline — it’s the thing that comes with the headline for free.

-----

**What `aiglos launch` actually does:**

```bash
pip install aiglos
aiglos launch
```

Five questions:

```
Agent name: scout
Tools you use: todoist, notion, netlify, github, twitter
Number of agents: 2 (CEO + Assistant)
Policy level: enterprise
Compliance: none / NDAA §1513 / CMMC
```

Generates the entire working directory in under 30 seconds:

```
.aiglos/
  soul.md          ← hard rules, no hallucinations, no slop phrases
  learnings.md     ← T79-protected, read-before-task instruction built in
  heartbeat.md     ← 30-min cadence, T67 monitoring registered automatically
  crons.md         ← heavy tasks template, separate from heartbeat
  tools.md         ← todoist, notion, netlify, github — hard-coded, no improvisation
  agents.md        ← CEO (Opus, push to main) + Assistant (Kimi, branches only)
  config.py        ← ready-to-run Aiglos guard, everything pre-wired
  SECURITY.md      ← what Aiglos is doing and why
```

Then runs GOVBENCH on day one to establish a baseline grade. The user starts with an A, and the system defends it.

-----

**Why each file is more than a template:**

**soul.md** — the tweet says “generate this using Claude based on your specific requirements.” `aiglos launch` does that automatically. The generated soul.md includes hard rules in T-taxonomy language: “No hallucinations” becomes `hard_bans=["fabricate_data", "unverified_claims"]`. “Verify every action” maps to Tier 3 gate configuration for destructive calls. The security rules the author had to know to write are pre-populated from the taxonomy.

**learnings.md** — the tweet’s most important file. Every mistake logged, read before every task, compounds over weeks. The security implication the author didn’t mention: if adversarial content gets written to learnings.md during a session, it gets injected into every future session. `aiglos launch` registers learnings.md with T79 protection automatically. The user gets the compounding memory benefit without the persistent foothold risk.

**heartbeat.md** — the tweet correctly separates heartbeat (lightweight, 30-60 min) from crons (heavy tasks, separate files). T67 HEARTBEAT_SILENCE already monitors heartbeat suppression. When `aiglos launch` creates heartbeat.md, it registers the expected cadence with T67. If the heartbeat stops at 3am, Aiglos knows immediately.

**tools.md** — “Todoist for tasks, Notion for docs, Netlify for deploys. Hard-coded.” This is the lockdown policy written in plain English. `aiglos launch` reads the tools the user declared, generates the lockdown allowlist automatically, and wires it to the guard. The user writes their tools once; Aiglos translates it into an enforcement boundary.

**agents.md** — CEO agent (Opus, push to main) + Assistant (Kimi, research only, side branches). `aiglos launch` generates the `declare_subagent()` calls from the description. CEO gets `scope_files=["main"]`, `tools=["git.push", "git.merge", "deploy"]`. Assistant gets `scope_files=["branches/"]`, `hard_bans=["push_to_main"]`. The two-agent trust boundary the author built manually is enforced automatically.

-----

**The tutorial page this creates:**

Two columns. Left: the tweet, step by step, with “a few hours to set up” at the bottom. Right: `aiglos launch`, 30 seconds, identical output with security built in. Every manual step the author described maps to one generated line.

The headline: “OpenClaw is the new computer. Aiglos makes it production-ready in 5 minutes.”

-----

**The distribution math:**

311,000 GitHub stars. The “useless out of the box” problem is universal — every new user hits it. The thread author spent a weekend on setup and got enough traction to write a public breakdown. That means there are tens of thousands of users who want this and don’t know how to build it.

If `aiglos launch` is the answer to that tweet — if the reply to “here’s how I spent my weekend” is “or just run this one command” — then Aiglos gets distribution from every OpenClaw onboarding conversation happening right now. The security is the moat. The setup wizard is the growth vector.

This is the NemoClaw parallel at the user layer. NemoClaw solved OpenClaw’s enterprise security problem with one command. Aiglos launch solves OpenClaw’s individual user setup problem with one command. Jensen validated the enterprise layer. The tweet validated the user layer.

-----

**Scope for the build:**

**Build 1 — `aiglos/cli/launch.py`** — the `aiglos launch` interactive setup wizard. Five questions, generates all six files, wires T67/T79/lockdown automatically, runs GOVBENCH baseline.

**Build 2 — template generator** — `soul.md`, `learnings.md`, `heartbeat.md`, `crons.md`, `tools.md`, `agents.md` templates with security pre-populated. The soul.md generator takes the declared tools and agent names and produces a document that reads like the tweet author wrote it, not like a config file.

**Build 3 — `aiglos agents scaffold`** — takes a plain-English description of the agent architecture and outputs `declare_subagent()` calls. “CEO agent, Opus 4.6, push rights to main” → the correct Python config.

**Build 4 — the tutorial page** — the tweet vs `aiglos launch` side-by-side. New page on the site: `tutorial-launch.html`.

All four in parallel. Want to go?​​​​​​​​​​​​​​​​