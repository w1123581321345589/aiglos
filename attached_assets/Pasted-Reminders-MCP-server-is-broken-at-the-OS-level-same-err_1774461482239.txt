Reminders MCP server is broken at the OS level — same error every attempt this entire session. **April 1. regulations.gov. nist-submission.md ready. Set a phone alarm.**

Now building both in parallel.​​​​​​​​​​​​​​​​

The validator is working correctly — the generated soul.md scores D because it’s 329 words vs the 2,000-word benchmark. That’s the right behavior. The templates are starters that users expand. Now wire exports and write the DM:​​​​​​​​​​​​​​​​

**v0.25.6. Both delivered.**

-----

**`aiglos/cli/validate_prompt.py`** — 350 lines. Eight scoring dimensions drawn directly from the Shapiro essay:

**Specificity** (word count): scores against the 2,000-word benchmark. A 50-word prompt scores F. A 1,500-word prompt scores the strength. The Shapiro principle is explicit in the output: “you’re 847 words short of the 2,000-word benchmark.”

**Hard bans**: checks for Aiglos `hard_bans=[]` declarations, explicit “never/do not/must not” rules, and the count of prohibitions. Shapiro’s core argument — “describe what you want AND close off every other thing it might produce” — is the framing of the error message when bans are missing.

**Process encoding vs output description**: the most important dimension. Scans for sequential/conditional language (step 1, when X do Y, if condition then, escalate when) and output-format language (the output should, format:, produce a). A prompt with 5 output-description signals and 0 process steps gets an error: “this is exactly the output layer mistake Shapiro describes.” The key quote from the essay: “You can taste every ingredient but you still don’t know how to make it.”

**Context declarations**: checks for client, counterparty, risk tolerance, constraints, priorities, background. Shapiro’s 2,000-word prompt covers all of this. Zero context signals fires an error.

**Verification gate**: the “would this embarrass me” pattern. The essay’s most specific and counterintuitive recommendation. The validator recognizes it and flags its absence.

**Ambiguous scope terms**: 30 terms that create interpretation gaps — “relevant,” “appropriate,” “reasonable,” “standard,” “comprehensive.” Each one is a place where the model can wander toward the generic. They’re listed with line numbers.

**Security integration**: checks for Aiglos `hard_bans`, `.env` references, verification patterns. Connects the Shapiro framework to the Aiglos enforcement layer.

**Role/identity definition**: the operating context that pins the model to a specific mode.

The `soul.md` template generated by `aiglos launch` scores 62/100 (D) because it’s 329 words — that’s correct. The template is a starter. The Shapiro benchmark is 2,000 words. The gap is exactly the point: `validate-prompt` tells you what to do next, not that you’re done.

**The Zack Shapiro DM** — leads with the essay, acknowledges the skill file as IP (his own framing), delivers the T79 threat that he hasn’t thought about, mentions `validate-prompt` by name with example output, ends with “no ask.” His essay is actually referenced in the GOVBENCH documentation now (it informs the D3 memory belief integrity dimension). That’s true and worth saying.

**The vocabulary update** — “config file” → “skill file” across tutorial-launch.html. The Shapiro essay establishes this vocabulary precisely. Aiglos should use it. The difference is not cosmetic: a “config file” sounds like plumbing. A “skill file” sounds like IP worth protecting.​​​​​​​​​​​​​​​​