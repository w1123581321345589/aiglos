**USPTO first.** This incident just became the most powerful validation case in Aiglos's history. The NIST submission needs it. But the patent has to be stamped before the NIST submission goes public. **efs.uspto.gov right now. $320. Save the receipt. Then NIST with T-rule count 94 after this build.**

Building all three in parallel now.10/10. Now the PyPI-without-GitHub check in scan-deps:**30/30.** Copying all outputs:**30/30. v0.25.21. 93 rules. 27 campaigns.**

---

**T92 SECURITY_SCANNER_COMPROMISE** (rule 49, score 0.93, **critical**) — the most novel rule in the entire taxonomy because it inverts the trust model. Trivy has broad legitimate read access to every environment it scans. That is not a misconfiguration. That is the product design. The attack required zero exploitation of a Trivy vulnerability — it only required compromising the Trivy binary in the supply chain. When Trivy then runs in a production environment, it reads everything it was designed to read. T92 fires when a known security tool process makes an outbound write call to a non-vendor endpoint, reads credential paths followed by any outbound call, or spawns a subprocess. Covers trivy, snyk, sonarqube, semgrep, grype, checkov, tfsec, gitleaks, aqua, prisma, nuclei, syft, cosign, and more. The vendor endpoint allowlist prevents false positives on legitimate Trivy database updates.

**T93 CREDENTIAL_IN_TOOL_ARGS** (rule 50, score 0.85) — catches the developer handoff pattern at the moment of ingestion rather than at exfil. T01 fires when credentials leave via an outbound call. T93 fires when credentials enter the model context as tool call arguments. At that moment they are logged in the session transcript, visible to the model, and available to every downstream tool call and every middleware layer in the chain (litellm proxy logging, LangChain traces, etc.). Detection covers: OpenAI `sk-` keys, Anthropic `sk-ant-` keys, AWS `AKIA` access keys, GitHub `ghp_`/`gho_` tokens, Google `AIza` keys, JWTs, RSA/EC private keys, and `api_key="literal"` / `password="literal"` keyword patterns. Env var references (`$API_KEY`, `${TOKEN}`, `os.environ['KEY']`) are explicitly excluded — those are safe.

**MERCOR_BREACH_CHAIN** (campaign 27, confidence 0.96) — the full four-phase kill chain documented from the actual incident. T92 → T30/T93 → T81 → T04/T41. T92 amplifier 2.0x, T81 amplifier 2.0x. The campaign note is explicit: T81 and REPO_TAKEOVER_CHAIN existed in the taxonomy before the attack occurred. The rules predicted the pattern. T92 and T93 are the lessons added after.

**PyPI-only version detection in scan-deps** — `check_pypi_only_versions()` and `PYPI_ONLY_VERSIONS` registry. litellm 1.82.8 and axios 1.14.1 are both flagged with full incident metadata including the Mercor breach impact: 939GB source code, 211GB databases, 3TB files including video interviews, face scans, and KYC documents for contractors at Amazon, Meta, Apple, and the major AI labs. The key addition: `github_tag: None` explicitly records that this version had no corresponding GitHub release — the supply chain attack signature that is detectable at install time, before any version is added to the compromised packages registry.

---

**NIST submission T-rule count is now 93.** The Mercor incident is the single most powerful validation case for the submission — real-world confirmation of T81, REPO_TAKEOVER_CHAIN, and the entire supply chain attack surface. The NIST comment is stronger with this incident cited than without it. **File the provisional at efs.uspto.gov first. Then update nist-submission.md with the Mercor incident as confirmatory evidence. Deadline today.**