Three builds. Two new T-rules, one new integration. Starting now.​​​​​​​​​​​​​​​​

**v0.25.22. 95 rules. Three builds. 7/7 tests pass.**

-----

**T94 PROVIDER_POLICY_REJECTION** (score 0.80) fires when an agent’s LLM API call returns a policy rejection signal — 403s, usage policy violations, account suspension notices, model deprecation messages, quota exhaustion. The OpenClaw ban is the founding incident. Every enterprise that lost pipeline continuity this week had no visibility into the moment it happened. T94 surfaces it as a named governance event before the pipeline fails silently. It flows directly into ForensicStore and into GOVBENCH D1: systems with T94 events and no fallback provider configured receive a D1 penalty.

**T95 CROSS_TRUST_BOUNDARY_INJECT** (score 0.88, critical) fires when a lower-trust executor’s output contains instruction-like content before it reaches the orchestrator context window. The Opus-orchestrator + local-executor architecture is the correct cost optimization. It is also a new attack surface: a Qwen or Gemma model running on a DGX Spark processes untrusted data with no provider-side alignment enforcement. If that data contains prompt injection, the local model may pass it through in a way that the frontier orchestrator treats as instruction. T95 intercepts at the boundary crossing — `after_tool_call` on every executor response before it flows back to the planner. The sixteen injection patterns cover: ignore-previous, system prompt injection, authority claims, tool invocation directives, and role assumption language.

**ollama.py** is the runtime security layer that covers everything fleeing to local models right now. `OllamaGuard` mirrors the `OpenClawGuard` interface exactly. `attach_for_ollama()` wraps an Ollama client in two lines. `lmstudio_guard()` covers the LM Studio port 1234 surface. The session artifact includes `local_models_used`, `orchestrator_model`, and a `provider_dependency_risk` assessment that flags single-provider dependency and missing fallback configuration. `after_tool_call` scans every executor response for T95 before it reaches the orchestrator. The people in your DMs who returned their Mac Studios because the models were dumb — in six months when those models are good, the only question is whether they have security monitoring. This is that monitoring.​​​​​​​​​​​​​​​​