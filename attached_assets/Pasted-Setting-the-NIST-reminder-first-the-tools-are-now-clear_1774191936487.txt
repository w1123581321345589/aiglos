Setting the NIST reminder first — the tools are now clearly in scope.​​​​​​​​​​​​​​​​

Still failing. Something is broken at the OS level in this environment — this has failed every single attempt this session. **Manual flag: April 1, regulations.gov, nist-submission.md ready.**

Now the honest answer on the build order:

-----

**4 of 5 built. Item 2 is partially built, item 5 is not built.**

**✓ 1 — `aiglos/cli/launch.py`** — built, 838 lines, working. The wizard runs 5 questions and generates all 7 files. `--from "description"` non-interactive mode works.

**⚠ 2 — `aiglos/templates/`** — the templates exist, but they’re embedded in `launch.py` as private functions (`_generate_soul_md`, `_generate_learnings_md`, etc.) rather than as a standalone `aiglos/templates/` directory. This matters because standalone template files are what the feature list promised — files that look like the tweet author wrote them, that users can inspect and customize independently of the Python code. The template functions are complete and produce the right output. The directory structure is missing.

**✓ 3 — `aiglos/cli/scaffold.py`** — built, 312 lines, working. NL → `declare_subagent()` translator.

**✓ 4 — `tutorial-launch.html`** — built, the comparison page with manual vs aiglos launch side-by-side.

**✗ 5 — Homepage CTA update** — not done. The homepage `index.html` still says `pip install aiglos`. It needs to say `pip install aiglos && aiglos launch`.

-----

Building the two missing pieces now:​​​​​​​​​​​​​​​​

All 5 items are now complete.

-----

**Build order reconciliation:**

**✓ 1 — `aiglos/cli/launch.py`** — built last session, 838 lines. The wizard, template generators, and `--from` non-interactive mode. All working.

**✓ 2 — `aiglos/templates/`** — just built. 8 files: `soul.md`, `learnings.md`, `heartbeat.md`, `crons.md`, `tools.md`, `agents.md`, `config.py`, `README.md`. Generated from a rich two-agent reference config (CEO + Assistant, Twitter/Notion/GitHub/Google Search). These are standalone files users can inspect, copy directly with `cp aiglos/templates/* .aiglos/`, or use as a reference when customizing what `aiglos launch` generates. The `README.md` in the templates dir maps each file to the T-rules it activates.

**✓ 3 — `aiglos/cli/scaffold.py`** — built last session, 312 lines. Plain English → `declare_subagent()` calls. Role-to-tools inference, model keyword detection, hard_ban population from role and branch scope.

**✓ 4 — `tutorial-launch.html`** — built last session. The comparison page: manual setup (tweet author’s steps, 3-4 hours) vs `aiglos launch` (same steps, automated, with the specific T-rule that’s active for each).

**✓ 5 — Homepage CTA** — just updated. `pip install aiglos` → `pip install aiglos && aiglos launch` in `index.html`. The CTA now reflects what the user actually does.

**The template quality note from the original scope:** “The files need to look like the tweet author wrote them.” Open `soul.md` and check — “No hallucinations. Verify and confirm every action before executing. If you don’t have data, say ‘data unavailable’ and fetch it. Never invent numbers.” That’s the tweet author’s language, not config file language. The `learnings.md` reads like a blank journal waiting to be filled in, not a YAML schema. `heartbeat.md` explicitly says “What heartbeat does NOT do” — the same structure the tweet author used to explain the distinction. The tone target was hit.​​​​​​​​​​​​​​​​